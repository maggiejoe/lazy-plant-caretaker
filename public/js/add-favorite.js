const userId = '1';
const results = document.querySelector(".results");

function addFavorite (event) {
    // get the plant id from the button that was clicked
    let plantId = event.target.value("answer-id");

    // Access user routes to add plant to their favorites
};

results.addEventListener("click", addFavorite);
